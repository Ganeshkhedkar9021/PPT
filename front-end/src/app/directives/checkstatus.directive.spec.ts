import { CheckstatusDirective } from './checkstatus.directive';

describe('CheckstatusDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckstatusDirective();
    expect(directive).toBeTruthy();
  });
});
